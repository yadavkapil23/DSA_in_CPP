#include<iostream>
using namespace std;
struct node{
    int data;
    struct node* left;
    struct node* right;
};
int main(){
    char ch;
    cout<<"do you want to enter the data in the tree : ";cin>>ch;
    while(ch=='y' || ch=='Y'){
        node* newnode = new node(); 
        
    }
}
